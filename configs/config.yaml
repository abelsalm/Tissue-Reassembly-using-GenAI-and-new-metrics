defaults:
    - _self_
    - validation: default
    - test: default
    - general: default
    - train: default
    - model: default
    - experiment: Perturb-FISH

hydra:
  job:
    chdir: True
  run:
    dir: ../outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}-${general.name}


